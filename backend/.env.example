# LangGraph Agent Builder — example env (full template: `lab init`, SPEC §14).
# All settings use the LAB_ prefix; every var has a CLI-flag twin.
LAB_DATABASE_URL=postgresql+asyncpg://lab:lab@localhost:55432/lab
# LAB_PORT=8010
# LAB_SECRET_KEY=                 # Fernet key; REQUIRED in prod
# LAB_HOST_URL=                   # public base URL for agent cards / file links
